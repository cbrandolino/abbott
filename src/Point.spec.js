/* eslint-disable prefer-arrow-callback */
import { expect } from 'chai';
import Point from './Point';

describe('Types: Point', function() {
  it('exists', function() {
    expect(Point).equal(1);
  });
});
